"""Phase 3 read-side helpers for chat features.

These are pure-Python functions that operate on a `ProjectedState`
snapshot. The hof-engine `@function` endpoints in
``app/domain/messages/functions.py``,
``app/domain/notifications/functions.py``, etc. wrap these for HTTP +
CLI exposure.
"""
